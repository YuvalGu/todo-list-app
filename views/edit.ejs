<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>Edit Task</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
  </head>
  <body class="container" style="margin-top: 50px">
    <% if (error.length) { %>
    <div class="alert alert-warning alert-dismissible" role="alert">
      <strong>ERROR: </strong><%= error %>
      <span
        type="button"
        class="close"
        data-dismiss="alert"
        aria-label="Close"
        style="position: absolute; right: 1rem"
        ><span aria-hidden="true">&times;</span></span
      >
    </div>
    <% } %>
    <div class="card">
      <div class="card-header">Edit Task</div>
      <div class="card-body">
        <form action="/update/<%= data.id %>" method="post">
          <div class="form-group" style="margin: 1em;">
            <label>Team Member:</label>
            <input
              type="text"
              class="form-control"
              name="team_member"
              autocomplete="off"
              value="<%= data.team_member %>"
            />
          </div>
          <div class="form-group" style="margin: 1em;">
            <label>Title:</label>
            <input
              type="text"
              class="form-control"
              name="title"
              autocomplete="off"
              value="<%= data.title %>"
            />
          </div>
          <div class="form-group dropdown" style="margin: 1em;">
            <label>Priority:</label>
            <select
              class="form-select"
              aria-label="Default select example"
              title="Pick priority"
              name="priority"
            >
              <option data-hidden="true">Pick priority</option>
              <option value="High" <%= data.priority == 'High' ? 'selected' : ''%>>High Priority</option>
              <option value="Middle" <%= data.priority == 'Middle' ? 'selected' : ''%>>Middle Priority</option>
              <option value="Low" <%= data.priority == 'Low' ? 'selected' : ''%>>Low Priority</option>
            </select>
          </div>
          <div class="form-group" style="margin: 1em;">
            <input type="submit" class="btn btn-info" value="Update" />
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
